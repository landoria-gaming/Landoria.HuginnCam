## Example BepInEx settings for the Valheim SagaCapture plugin.

[Controls]

## Shortcut used to enter or leave CaptureMode.
## https://docs.unity3d.com/ScriptReference/KeyCode.html
CaptureModeShortcut = F8

[CinematicCameraRendering]

## Valheim graphics preset used to render the cinematic camera before video encoding:
## SameAsGame, VeryLow, Low, Medium, or High.
GraphicsPreset = SameAsGame

## Internal cinematic-camera resolution: SameAsGame, Preset (graphics preset value),
## HD720 (1280x720), FullHD1080 (1920x1080), QHD1440 (2560x1440),
## or UHD2160 (3840x2160).
Resolution = SameAsGame

## Frame rate shared by CaptureMode, the Unity game loop,
## and the video recorder: 30 or 60 FPS. VSync is temporarily disabled
## so Unity does not render more frames than the recorder accepts.
MaximumFrameRate = 60

[GameplayCamera]

## Include Valheim's interface in gameplay-camera shots.
## When false, capture occurs before overlay UI is rendered.
IncludeUI = false

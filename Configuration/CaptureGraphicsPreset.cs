namespace Landoria.SagaCapture
{
    // Selects the Valheim graphics preset used by the capture camera.
    internal enum CaptureGraphicsPreset
    {
        SameAsGame,
        VeryLow,
        Low,
        Medium,
        High
    }
}
